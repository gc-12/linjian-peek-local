User: 参考 https://github.com/LoverConnect/LoverConnect 和 https://github.com/AZHi-xinxin/LoverConnect-Enhanced ，如何将MCP服务直接打包进apk中，在app页面点击启动服务即可在手机本地localhost特定端口开放MCP服务器？
我想要修改 https://github.com/linzhi-524/linjian-peek-public ，实现上述功能，并且便于同步上游更新，构建新版app，应该怎么做？

# 把 MCP 服务打包进 APK 并保持上游可同步的完整方案

**结论先行**：在 Android APK 内把 MCP 服务暴露在 `127.0.0.1` 完全可行，最优路径是用 **MCP 官方 Kotlin SDK (`io.modelcontextprotocol:kotlin-sdk`) + 内嵌 Ktor CIO 引擎 + 前台 Service** 的组合，在 App 内启动一个只监听 loopback 的 Streamable HTTP MCP 服务；**不建议**走 Termux/Node.js 嵌入路线—— linjian-peek-public 的 `mcp/server.js` 逻辑量级不大，用 Kotlin 重写为原生实现比在 APK 里塞一个 Node 运行时（几十 MB、兼容性差、保活更难）划算得多。修改方式上，**新建 Gradle 子模块 `android/mcplocal/` 承载全部本地 MCP 代码**，上游的 `server/`、`mcp/`、`docs/`、`android/app/` 不动，配合 GitHub Fork + upstream remote，合并冲突几乎为零。

## 一、先看清 linjian-peek-public 的现状与改造点

该仓库是三件套结构：

- `android/`：Android 客户端（含 `build.sh`、签名证书 `signing/zhangxinchuang-public-release.p12`、GitHub Actions 构建工作流）
- `server/`：Python 同步后端，负责 `/health`、`/api/poll`、`/api/screenshot` 等中转
- `mcp/`：**Node.js 写的 MCP 服务**（仅 `package.json` + `server.js`），通过 `LINJIAN_URL` 反连 server，再由 server 轮询把命令下发给手机
  也就是说，目前"截图、读屏、点击、专注模式、小金库"这些 MCP 工具的调用链是：**AI 客户端 → 远程 MCP(Node) → 远程 server → HTTP 轮询 → 手机 App → 无障碍/截图服务**。
  把 MCP 塞进手机本地后，这条链可以大幅简化为：**AI 客户端 → `127.0.0.1:PORT/mcp` → App 内 Kotlin MCP Server → 直接调用无障碍/截图/通知等本机能力**。状态类工具（日记、小金库、守护日历）可以继续走原 server 做云同步，也可以先用本地存储兜底——建议保留原 server 作为同步/备份通道，这样 `server/`、`mcp/` 目录完全不必动，上游更新时零冲突。
  LoverConnect（Kotlin + Jetpack Compose）就是这么做的：启动后在本地开 MCP Streamable HTTP 服务，地址 `http://127.0.0.1:5000/mcp`，工具直接读电量、步数、前台 App 等本机状态。它的 Enhanced 版还补了三项值得抄的安全设计：**只监听 `127.0.0.1`、拒绝浏览器 Origin、每次安装生成随机路径令牌**（形如 `http://127.0.0.1:5000/mcp/<random-token>`），避免同机其它 App 或局域网直接读到 MCP 数据。

---

## 二、MCP 嵌入方案对比与选型

| 方案                                    | 实现方式                                                                                               | 优点                                     | 缺点                                                   | 是否推荐       |
| ------------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------- | ---------------------------------------------------- | ---------- |
| **MCP Kotlin SDK + 内嵌 Ktor CIO**      | JVM 原生，前台 Service 持有 Ktor `embeddedServer(CIO, host="127.0.0.1", port=…)`，挂 `mcpStreamableHttp` 路由 | 体积小（几 MB）、协程友好、与 App 共享无障碍/截图上下文、官方维护  | 需要 Kotlin 重写工具逻辑                                     | ✅ **强烈推荐** |
| NanoHTTPD + 自己实现 MCP 协议               | 手写 JSON-RPC / SSE                                                                                  | 极轻                                     | 要自己追 MCP 协议演进（initialize/tools/notifications/SSE 升级） | 备选         |
| WebView / Chaquopy 嵌 Node             | 在 APK 里跑 `mcp/server.js`                                                                           | 复用上游 mcp 代码                            | Node 二进制 +30MB、Android 兼容性差、保活更难、终端用户难装              | ❌ 不推荐      |
| Termux 依赖运行                           | 让用户装 Termux + Node                                                                                 | 无需改 APK                                | 部署复杂，体验割裂，违背"点击启动"目标                                 | ❌ 不推荐      |
| 现成 `dev.jasonpearson:mcp-android-sdk` | 封装 MCP Kotlin SDK 的 Android 库                                                                      | 开箱即用，自带 WebSocket 8080 / HTTP-SSE 8081 | 默认端口写死、抽象层较厚                                         | 可作为参考实现    |

---

## 三、改造成 linjian-peek-local 的具体步骤

### 步骤 1：Fork 仓库并配置 upstream

```bash
# 在 GitHub 网页 Fork linzhi-524/linjian-peek-public → 你的账号
git clone https://github.com/<你>/linjian-peek-public.git
cd linjian-peek-public
git remote add upstream https://github.com/linzhi-524/linjian-peek-public.git
git remote -v   # 验证 origin + upstream
```

### 步骤 2：新建独立 Gradle 模块 `android/mcplocal/`

目录结构（**全部新增内容都放这里**，上游目录一行不改）：

```
android/
├── app/                    # 上游原有，不动
├── mcplocal/               # ← 新增模块
│   ├── build.gradle.kts
│   └── src/main/
│       ├── AndroidManifest.xml
│       └── java/.../mcplocal/
│           ├── McpLocalService.kt
│           ├── McpToolRegistry.kt
│           └── tools/      # 各工具实现
└── signing/                # 上游签名，绝对保留（否则老用户无法覆盖安装）
```

`android/mcplocal/build.gradle.kts`：

```kotlin
plugins { id("com.android.library"); id("org.jetbrains.kotlin.android") }
android {
    namespace = "com.linjian.mcplocal"
    compileSdk = 36
    defaultConfig { minSdk = 26 }
    kotlinOptions { jvmTarget = "17" }
}
dependencies {
    implementation("io.modelcontextprotocol:kotlin-sdk:0.11.+")   // 官方 MCP Kotlin SDK
    implementation("io.ktor:ktor-server-cio:3.2.3")               // Android 上推荐 CIO，轻量省电
    implementation("io.ktor:ktor-server-core:3.2.3")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.10.+")
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.7.+")
}
```

`android/app/build.gradle.kts` 加一行：`implementation(project(":mcplocal"))`；`android/settings.gradle.kts` 加 `include(":mcplocal")`。

### 步骤 3：Manifest 与网络安全配置

`android/app/src/main/AndroidManifest.xml` 中追加（尽量用 `tools:node="merge"` 保持合并友好）：

```xml
<uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.WAKE_LOCK"/>
<application android:networkSecurityConfig="@xml/network_security_config">
    <service
        android:name="com.linjian.mcplocal.McpLocalService"
        android:exported="false"
        android:foregroundServiceType="dataSync"/>
</application>
```

`android/app/src/main/res/xml/network_security_config.xml`（Android 9+ 默认禁明文，需为回环地址放行，避免客户端连 `http://127.0.0.1` 报 `Cleartext HTTP traffic not permitted`）：

```xml
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
```

### 步骤 4：核心 Service 与 MCP Server 骨架

`McpLocalService.kt` —— 仿照 LoverConnect 的"点击启动 → 前台服务 → 启动 Ktor → 暴露 `/mcp`"模式：

```kotlin
class McpLocalService : Service() {
    private var server: EmbeddedServer<*, *>? = null
    private val port = 5000  // 可从 SharedPreferences 读取，被占时自动 +1
    override fun onStartCommand(i: Intent?, f: Int, id: Int): Int {
        startForegroundCompat()                       // 起通知，保活关键
        if (server == null) startMcp()
        return START_STICKY
    }
    private fun startMcp() = CoroutineScope(Dispatchers.IO).launch {
        val mcp = Server(
            serverInfo = Implementation("linjian-local-mcp", "0.3.8.4-local"),
            options = ServerOptions(capabilities = ServerCapabilities(
                tools = ServerCapabilities.Tools(listChanged = true)))
        )
        McpToolRegistry.registerAll(mcp, applicationContext)   // 见步骤 5
        val token = randomToken()  // 仿 LoverConnect-Enhanced 的随机路径令牌
        server = embeddedServer(CIO, host = "127.0.0.1", port = port) {
            intercept(ApplicationCallPipeline.Setup) {
                if (call.request.origin.remoteHost != "127.0.0.1") call.respond(HttpStatusCode.Forbidden)
                if (!call.request.path().startsWith("/mcp/$token")) call.respond(HttpStatusCode.NotFound)
            }
            mcpStreamableHttp { mcp }          // Streamable HTTP 传输
        }.start(wait = false)
        notifyAddress("http://127.0.0.1:$port/mcp/$token")   // 在通知/设置页展示完整地址
    }
    override fun onDestroy() { server?.stop(1000, 2000); server = null }
    override fun onBind(i: Intent?): IBinder? = null
}
```

### 步骤 5：把上游 MCP 工具本地化

上游 `mcp/server.js` 里的工具（截图、读屏、点击、输入、通知、天气、日历、归电、门禁、屏幕休息、专注模式、日记、小金库、外卖助手等），迁移策略分两类：

- **设备直采类**（截图、读屏、前台 App、通知、电量、媒体播放、专注模式）：不再走 server 轮询，**直接在 Kotlin 里调用 App 已有的无障碍服务 / MediaProjection / UsageStatsManager**。这些服务的实例获取方式参考 `android/app/src/main` 里现有代码——通常通过 `AccessibilityService` 全局实例或绑定 Service 拿。

- **状态云同步类**（日记、小金库、守护日历、到访记录）：保留调用原 server 的 `/api/*`（HTTP 客户端在 `McpLocalService` 里复用 App 现有的 Retrofit/OkHttp 配置），这样上游 server 协议升级时你只需在 Kotlin 里改 URL/参数，逻辑跟得上。
  `McpToolRegistry.kt` 的注册模板（与官方 SDK 文档一致）：
  
  ```kotlin
  object McpToolRegistry {
    fun registerAll(server: Server, ctx: Context) {
        server.addTool(
            name = "get_phone_state",
            description = "读取当前前台 App、电量、媒体播放等",
            inputSchema = ToolSchema(properties = buildJsonObject {})
        ) { _ ->
            val state = PhoneStateBridge.read(ctx)  // 直接读无障碍/UsageStats
            CallToolResult(content = listOf(TextContent(state)))
        }
        server.addTool(
            name = "take_screenshot",
            description = "请求授权截图",
            inputSchema = ToolSchema(properties = buildJsonObject {
                put("quality", buildJsonObject { put("type", "string") })
            })
        ) { req -> CallToolResult(content = listOf(TextContent(ScreenshotBridge.capture(ctx, req)))) }
        // ... 其余工具同上逐个迁移
    }
  }
  ```
  
  ### 步骤 6：设置页加"启动本地 MCP"开关
  
  在 `android/app/src/main/.../SettingsActivity`（或对应的 Compose 设置页）加一个 Switch + 地址展示卡片，仿照 LoverConnect 的权限按钮行设计：
  
  ```kotlin
  var mcpRunning by remember { mutableStateOf(McpLocalBridge.isRunning()) }
  Switch(checked = mcpRunning, onCheckedChange = { on ->
    if (on) ContextCompat.startForegroundService(ctx, Intent(ctx, McpLocalService::class.java))
    else ctx.stopService(Intent(ctx, McpLocalService::class.java))
    mcpRunning = on
  })
  Text("MCP 地址：${McpLocalBridge.currentUrl}，长按复制")
  ```
  
  同时在该页把"服务器地址"的默认值改为 `http://127.0.0.1:5000`，**保留可改回远程地址的输入框**作为兼容模式——这样即便本地 MCP 启不来，App 仍可用上游方案。

---

## 四、同步上游 + 构建新版 APK

```bash
git fetch upstream
git checkout main
git merge upstream/main          # 因新增内容全部在 mcplocal/ 新目录，冲突面≈0
# 若上游改动了 android/app（如设置页/Manifest），用 IDE 手工合入你的几行 diff 即可
git push origin main
```

GitHub Fork 仓库默认会带 Actions：进入你的仓库 **Actions → Build Android Public APK → Run workflow**，等待产物 `zhangxinchuang-public-v0.3.8.4-apk` 下载。如果想连本地 MCP 工具一并发布，可在 `.github/workflows` 的构建脚本里追加 `./gradlew :android:app:assembleRelease`（路径以仓库实际为准）确保 `mcplocal` 被一起编译。
**签名**：保留上游 `android/signing/zhangxinchuang-public-release.p12` 不变即可无缝覆盖老版本；如果你打算长期独立维护，可以生成自己的签名，但从此不能与上游 APK 互覆盖，建议在 README 标明。

---

## 五、高频踩坑清单

1. **端口被占**：5000 可能被其它 App 抢，启动前 `ServerSocket(0)` 探测空闲端口，把实际地址回写到设置页与通知。
2. **后台被杀**：Android 8+ 普通后台 Service 几分钟就会被冻结，必须用**前台 Service + 通知**并申请 `FOREGROUND_SERVICE_DATA_SYNC` 类型（API 34+ 强制）；引导用户关闭电池优化、允许后台运行（OV 米需要专门设置，LoverConnect 文档也反复强调这点）。
3. **明文 HTTP 被拒**：`network_security_config.xml` 必须包含 `127.0.0.1` 放行，否则同 App 内客户端调用本地 MCP 都会抛异常。
4. **同机其它 App（如 RikkaHub）连不上**：连接方 App 自己的 manifest 决定是否允许明文，你无法修改；LoverConnect-Enhanced 的做法是把完整地址（含随机 token）显示给用户复制，遇到 `CLEARTYPE not permitted` 时让对方在客户端配置里开启"允许 HTTP"或使用支持 loopback 的 MCP 客户端。
5. **Origin / 安全**：照搬 LoverConnect-Enhanced 的三条：只监听 `127.0.0.1`、拒绝浏览器 Origin（Ktor `intercept` 中检查 `Origin` 头）、路径加随机 token。
6. **签名兼容**：`android/signing/` 下证书不动，避免老用户被迫卸载重装。
7. **MCP 协议演进**：`io.modelcontextprotocol:kotlin-sdk` 仍在 0.x 阶段，升级时留意 Ktor 版本匹配（曾有 0.8.3 与 Ktor 3.4.0 的 SSE 兼容 issue），建议在 Gradle 用固定版本 + `.+` 小版本。

---

## 最小代价落地顺序

1. Fork + `remote add upstream`（5 分钟）
2. 新建 `android/mcplocal` 模块，复制 LoverConnect 的 Service/工具注册模式骨架（半天）
3. 迁移 1–2 个高频工具（如 `get_phone_state`、`take_screenshot`）跑通端到端（1 天）
4. 设置页加启动开关 + 地址复制（半天）
5. 之后每次 `git fetch upstream && git merge upstream/main` + 跑 Actions，即可吃到上游新功能而几乎不产生冲突。
